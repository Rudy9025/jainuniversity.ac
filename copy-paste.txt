import pandas as pd

# Load your CSV file
df = pd.read_csv("your_file.csv")  # Replace with the actual file path

# Create the new column
df['CRDS Party + 3 + Post Direction'] = df['CRDS Party Code'].astype(str) + '3' + df['Post Direction'].astype(str)

# Save to a new file or view the result
df.to_csv("updated_file.csv", index=False)
print(df[['CRDS Party Code', 'Post Direction', 'CRDS Party + 3 + Post Direction']].head())